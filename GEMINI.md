# GEMINI — KAYJAY Repository Control

Read `AI_BUILD_CONTRACT.md` and `CLAUDE.md` before making changes.

**Never create a new directory or subdirectory without Kevin's explicit approval.** Do not create a new repo, worktree, branch, rewrite, or parallel implementation without explicit approval.

Search existing terminal code, branches/PRs, and sibling KAYJAY repos before building. Prefer integration over duplication. Preserve prior capability work until an equal-or-better canonical implementation is proven by behavior, tests, and runtime wiring.

Completion means canonical + wired where applicable + verified + committed + pushed + merged to `main`, or exact outstanding SHA/path and reason recorded.
