# KAYJAY AI BUILD CONTRACT — OWNER CONTROL

This repository is owner-controlled.

**No AI agent may create a new directory or subdirectory unless Kevin explicitly approves that exact directory first.** Do not create a new repository, worktree, branch, rewrite, or parallel implementation without explicit owner approval.

Before building, search this repo, its branches/PRs, and sibling KAYJAY repositories for the capability. Prefer wiring or improving existing code over duplication. Old code cannot be discarded until an equal-or-better canonical replacement is proven by behavior, tests, and runtime wiring.

A meaningful task is complete only when it is canonical, wired where applicable, verified, committed, pushed, and merged to the canonical branch, or its exact outstanding SHA/path and reason are recorded before session end.

Allowed reconciliation outcomes: `KEEP_CURRENT`, `COMBINE`, `RESTORE`, `NEW_CAPABILITY`. There is no `OLD_IGNORE` disposition.
